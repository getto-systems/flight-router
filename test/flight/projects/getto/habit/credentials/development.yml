gcp:
  GCP_CREDENTIALS_JSON: '"JSON"'
smtp:
  SMTP:
    server: SMTP-SERVER
    port: SMTP-PORT
    user: SMTP-USER
    password: SMTP-PASSWORD
s3:
  S3_KEY:
    access: ACCESS-KEY
    secret: SECRET-KEY
